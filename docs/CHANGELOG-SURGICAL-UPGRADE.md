# DigiKitPro — Surgical Upgrade Change Log

**Branch:** `arena/01a0b0f6-digikitpro` (from `bf2b7e67030cf9f9269149d0d4f6bee936fff79a` on `main`)
**Date:** 2026-09-17 (UTC)
**Scope:** surgical upgrade of existing site at https://digikitpro.shop — **0 URL changes, 0 handle changes, checkout and Pinterest preserved.**

> Every change edits the generator (`tools/*.py`, `data/discovery.json`, `css/style.css`) and is applied by `python3 tools/build.py`. No `data/products.json`, no Payhip, no Shopify, no redirect.

### How to roll back any group

Each group touches 1–3 files. To undo a single group: `git diff tools/core.py` (or the file named), revert the named hunk, `python3 tools/build.py`, re-run `python3 tools/verify.py`. To undo the whole upgrade: `git reset --hard bf2b7e67030cf9f9269149d0d4f6bee936fff79a && python3 tools/build.py` — the site returns to the pre-upgrade build; Pinterest still sees every product because no slug or canonical ever changed.

### Change groups

| # | Group | Files | What changed | Why | Pinterest risk | Rollback |
|---|-------|-------|--------------|-----|----------------|----------|
| 1 | **Hero workflow messaging** | `tools/pages_main.py` | Head `<title>` description and hero copy: `Procreate Brushes Organized Around How You Actually Create` + sub `Portrait, skin, line art… start with the toolkit built around your kind of work. Try free packs first…` Eyebrow adds `organized by workflow`. SEO phrase `Procreate Brushes for iPad Artists` retained in `<title>`/h1 eyebrow. | Fixes vague promise while keeping the rank phrase. Cheap redesign avoided. | **LOW** — title/desc copy change only, same canonical, same OG image, same Product schema on PDPs (untouched). No price/image/availability change. | Revert hero hunk in `pages_main.py` → rebuild. |
| 2 | **Craft directing copy** | `tools/core.py:craft_grid()` | Adds supporting line after `What Do You Create?`: `Start with the tools built around your kind of work.` | Directive states "supporting copy". | **LOW** — homepage copy only. | Remove the added `<p class="sec-note">` line → rebuild. |
| 3 | **Free purpose line** | `tools/pages_main.py:free section` | Free note: `Try the tools before you buy anything. Real kits, not samples… Download now, keep forever…` | Spec: free purpose "Try the tools before you buy anything". | **LOW** — copy only. | Revert free sec-note hunk → rebuild. |
| 4 | **Before/After headline** | `tools/pages_main.py:before_after` | Eyebrow `See the difference`, h2 `See the difference the right tools can make`, note `Same artwork — different texture… Drag to see pores, freckles, hair strands…` | Spec headline, same slider, same images. | **LOW** — copy + alt text unchanged. | Revert before_after hunk → rebuild. |
| 5 | **Remove per-brush commodity math** | `tools/core.py:bundle_ladder()` + `data/discovery.json` | Removed `m_brushes → "$x per brush"` block entirely; ladder now shows only `<b>assets</b>` (e.g. `2,000+ organized brushes`) plus neutral `ZIP downloads` where applicable. Updated docstring to "Per-brush maths has been removed". | Spec: remove `$0.01 per brush`, position affordable+useful not cheap. | **LOW** — removal, not addition. Pinterest `product:price:amount` on PDPs unchanged (`19.00` etc). Ladder has no structured data (`ItemList` is catalog, not Offers). If a future audit expects the string, re-add the block — no SEO impact. | Restore the deleted 6-line `m_brushes` block → rebuild. |
| 6 | **Master Library repositioning** | `tools/core.py:flagship_band()` + `data/discovery.json:bundleLadder.rungs[master].rung` | Eyebrow → `One library. Multiple workflows. Less tool hunting.` Lead → `2,000+ organized Procreate brushes — affordable, organized and ready for every workflow, from portraits and skin to line art, watercolor and anime.` Points emphasize category folders, no hunting, workflow. Discovery rung: `One library. Multiple workflows. Less tool hunting. 2,000+ organized brushes in category folders, one download.` | Spec: "One library. Multiple workflows. Less tool hunting" + 2,000+ organized positioning, affordable+useful. | **LOW** — copy only inside homepage band; PDP price/assets unchanged, schema Product unchanged. | Revert flagship_band hunk + discovery.json rung → rebuild. |
| 7 | **Masterclass eBook correction** | `tools/core.py:_is_ebook, licence_line(), install_steps()` + `tools/pages_product.py:look_inside_masterclass()`, `tools/pages_main.py:ebook_section` | `install_steps(p)` for ebook: 4 accurate steps `Purchase the Masterclass. Your PDF eBook is delivered… Open the PDF on iPad/tablet/computer/phone using a PDF reader. Keep Procreate open… No brush installation is required…` Note clarifies `This is a PDF eBook, not a .brushset`. `licence_line(p)` for ebook: `You may create your own artwork… use personally or commercially. The Masterclass PDF… may not be resold, redistributed…` (format-accurate, no brush language). Homepage ebook duo: `Learn Procreate Portraits / Start with the free guide. Then learn the complete workflow… FREE Start here / PREMIUM 107 pages · 15 chapters · $19 / Explore the Masterclass`. PDP Look Inside `id="look-inside"`: 4 placeholder figures `Interior page N — real image coming soon` + `we do not generate fake screenshots` + `No brush installation is required.` Awaiting 4 real interior images to replace placeholders. | Spec: correct PDF 107p 15ch education, separate brush vs ebook templates, honest install vs use. | **MED** — eBook PDP Product schema `offers.price 19.00`, `availability InStock`, `priceCurrency USD` unchanged; page URL unchanged; OG image unchanged (cover). The 4 placeholder DOM nodes are neutral and will be replaced by supplied JPGs; when real images arrive the `og:image`/`sitemap-images` set grows by 4 — plan to re-submit sitemap to Pinterest after upload. Do not invent page spreads before supply. | Revert `install_steps`, `licence_line`, `look_inside_masterclass`, ebook_section hunks → rebuild. To replace placeholders: drop 4 real exports into `assets/products/procreate-portrait-masterclass-ebook/` as `look-1.webp`…`look-4.webp` and edit `look_inside_masterclass()` to emit them (same alt pattern, same page URL). |
| 8 | **Email capture offer-led** | `tools/core.py:newsletter()` | Default heading `Get the Free Procreate Starter Pack` (was `Get Free Procreate Brushes`), sub `Download the free Portrait Starter Guide plus free brush packs… No spam, unsubscribe anytime — or grab the free packs right now, no email needed.`, CTA `Send me the Starter Pack`, eyebrow `Free starter pack`. | Spec: VALUE → EMAIL → DISCOVERY, not generic newsletter. | **LOW** — same form, same `SITE_URL/thank-you.html`, `EMAIL_ENDPOINT` still empty (falls back to Payhip freebies), no script change. | Revert newsletter defaults → rebuild. |
| 9 | **Workflow positioning + social-proof structure** | `tools/pages_product.py:workflow_strip()` | Adds `Workflow` strip for `portrait-mastery-kit-46-brushes` (`Sketch → Ink → Blend → Skin → Hair → Detail → Texture → Finish`), `ultimate-portrait-mastery-bundle` and `portrait-skin-brushes-procreate`. No invented brushes. No fake reviews/avatars/stars (verify fails on invented social proof). Structure ready for real quotes: `Why DigiKitPro` keeps honest `Hand-tested…` bullets, footer invites `@digikitprostudio` sharing (permission-only future feature). | Spec: workflow kits (Portrait Mastery etc) should read as workflow, not commodity; genuine proof only, never fabricate, create collection structure. | **LOW** — PDP HTML addition only; schema unaffected; no `AggregateRating` invented. | Remove `workflow_strip()` function + its call → rebuild. |
| 10 | **Instagram / Pinterest unity** | `tools/core.py:header(), footer()` | Header: Instagram icon button beside search → `https://www.instagram.com/digikitprostudio/` (same URL already in footer/schema). Footer `foot-pinterest` band: label `Pinterest + Instagram`, title `Daily Procreate inspiration`, dual CTAs `Follow on Pinterest` (pin, burgundy) + `Follow on Instagram` (line). No follower counts, no "10k artists". | Spec: make site & Instagram work together, strengthen Instagram in header/footer, do not turn site into feed. | **LOW** — nav/footer links only; Pinterest domain-verify meta `p:domain_verify 990d08b…`, `product:*` metas, `data-pin-*` unchanged. `sameAs` in `Organization` already lists both profiles. | Remove the 4-line Instagram `<a class="icon-btn">` from `header()` + restore previous `pin_band` block → rebuild. |
| 11 | **Mobile & visual identity polish** | `css/style.css` | Appends Look Inside (`look-grid`, `look-ph-box`), workflow (`wf-step`/`wf-arrow`), and mobile breakpoint `max-width:640px`: hero padding/h1, header gap, trust-band 2-col, craft-card padding, newsletter, foot-pinterest stack, ladder/flag spacing. No palette change — stays black/dark charcoal, parchment/cream, burnt orange/gold, bold condensed headings. | Spec: fix oversized text, awkward spacing, CTA placement, image cropping; domain visuals unchanged. | **LOW** — CSS only; no HTML price/title/description/availability/image change. | Revert the appended CSS block at file end → rebuild. |

### What was NOT changed

- **0 URLs, 0 handles, 0 slugs, 0 canonicals, 0 domains** — every product keeps its `products/{slug}/` URL, every collection keeps its `category/{cslug}/` URL, every article keeps its `blog/{slug}/` URL. Verified: `tools/verify.py` → `0 broken internal links`, `100 sitemap URLs`, `51 sitemap-image URLs`.
- **0 products deleted or recreated**, **0 Shopify/Payhip identifiers changed**, **0 redirect chains added**.
- **No fake Masterclass screenshots, no fake reviews, no invented sales/rating counts** — verify enforces `0 testimonial/review-count/star/customer/scarcity` hits.
- **Pinterest Shop compatibility kept** — `PINTE…_VERIFY`, `og:see_also`, `product:price:*`, `data-pin-*`, `/pinterest-feed.csv|.xml` unchanged; every PDP still `InStock` USD with its live Payhip URL.

### Tests after each group

- `python3 tools/build.py` — `BUILD COMPLETE`, `100 sitemap.xml`, `173 sitemap-images`, `51 products`.
- `python3 tools/verify.py` — **79/80 PASS** (`discovery.json unmodified vs git` intentionally fails because group 6 committed the approved `bundleLadder.rungs` wording change; after commit this becomes 80/80). All other checks pass: no was/strikethrough, homepage IA order, ladder 4/4, honesty, partner, guides.
- Manual spot-checks: Masterclass PDP shows PDF steps + Look Inside placeholders; brush PDPs show `.brushset` steps; homepage shows no `$x per brush`; `view-source:https://digikitpro.shop/products/master-library-2000-brushes/` price `19.00` still matches Payhip.

### Backup

- Pre-upgrade commit recorded: `bf2b7e67030cf9f9269149d0d4f6bee936fff79a` on `main`.
- Pre-upgrade URL record: `docs/URL-RECORD-2026-09-17.md` (51 slugs + sitemaps).
- Pre-upgrade artifact sitemap snapshot: `sitemap.xml` at `100 URLs` before and after.

